name: link_preview
description: A flutter library to show preview of links using OEmbed.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

